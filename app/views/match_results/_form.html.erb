<%= simple_form_for(@match_result) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :applicant %>
    <%= f.association :program %>
    <%= f.association :university %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
