<%= content_for :header do %>
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
      <h1 class="h3 mb-0 text-gray-800"><%= header rescue nil %></h1>
    </div>
<% end %>
<div class="card shadow mb-4">
  <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
    <h6 class="m-0 font-weight-bold text-primary">Participant Types</h6>
  </div>
  <!-- Card Body -->
  <div class="card-body">
    <table class="table table-bordered table-striped table-hover dataTable js-exportable">
      <thead>
      <tr>
        <th>Name</th>
        <th>Description</th>
        <th></th>
      </tr>
      </thead>

      <tbody>
      <% @participant_types.each do |participant_type| %>
          <tr>
            <td><%= participant_type.name %></td>
            <td><%= participant_type.description %></td>
            <td>
              <%= link_to 'Edit', edit_participant_type_path(participant_type) %> |
              <%= link_to 'Delete', participant_type, method: :delete, data: { confirm: 'Are you sure?' } %>
            </td>
          </tr>
      <% end %>
      </tbody>
    </table>
    <hr/>
    <%= link_to 'New Participant Type', new_participant_type_path, class: 'btn btn-primary' %>
  </div>
</div>